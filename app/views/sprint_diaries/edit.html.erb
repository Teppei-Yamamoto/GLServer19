<div class="page-header">
  <%= link_to scrum_member_sprint_diaries_path, class: 'btn btn-default' do %>
    <span class="glyphicon glyphicon-list-alt"></span>
    Back
  <% end %>
  <%= link_to scrum_member_sprint_diary_path(@scrum_member, @sprint_diary), class: 'btn btn-primary' do %>
    <span class="glyphicon glyphicon-info-sign"></span>
    Show
  <% end %>
  <h1>Editing sprint_diary</h1>
</div>

<%= render 'form' %>
